{
  "name": "chromaghost",
  "version": "0.1.0",
  "private": true,
  "description": "Photoshop UXP plugin: grayscale to full color pass with hierarchical zones, materials, and accents.",
  "scripts": {
    "build": "webpack --mode=production",
    "package": "node scripts/package-ccx.js",
    "build:package": "npm run build && npm run package",
    "watch": "webpack --mode=development --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "adm-zip": "^0.5.17",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
